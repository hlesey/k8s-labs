apiVersion: v1
kind: Pod
metadata:
  name: nginx-basic-pod
spec:
  containers:
    - name: nginx
      image: nginx:1.7.9
      ports:
        - name: http
          protocol: TCP
          containerPort: 8080

