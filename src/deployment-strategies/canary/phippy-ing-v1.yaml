---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: phippy
spec:
  rules:
  - host: phippy.local
    http:
      paths:
      - path: /
        backend:
          serviceName: phippy-v1
          servicePort: 80
